---
layout: layouts/base.njk
---
<article class="job-detail">
  {% set brandRecord = collections.brands | brandBySlug(brand) %}
  <a class="back-link" href="/#jobs">← All jobs</a>
  <header>
    {% if brandRecord %}
      <a class="job-brand" href="{{ brandRecord.url }}">
        <img src="{{ brandRecord.data.logo }}" alt="" eleventy:widths="96,192" eleventy:formats="svg,webp" sizes="96px">
        <span>{{ brandRecord.data.name }}</span>
      </a>
    {% endif %}
    <h1>{{ title }}</h1>
    <p class="job-detail-meta">{{ location }} · {{ workplace }} · {{ employment_type }}</p>
    {% if salary %}<p>{{ salary }}</p>{% endif %}
  </header>
  <div class="prose">{{ content | safe }}</div>
  <a class="button button--dark" href="{{ apply_url }}">Apply for this role</a>
</article>
